# regui changelog

## 0.1.0

- Initial release
- `Regui`: run part of a ui in its own egui viewport and paint it into the parent, scaled,
  rotated or offset
- `BackdropBlur` (`wgpu` feature): blur whatever is already drawn behind a rect
